version: '3.8'

services:
  api:
    build: .
    ports:
      - '3001:3001'
    env_file:
      - .env
    restart: unless-stopped

  # Uncomment when adding a database:
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: soroprotocol
  #     POSTGRES_USER: soro
  #     POSTGRES_PASSWORD: soro
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data

# volumes:
#   pg_data:
